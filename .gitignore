# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Node (only used for tests / dev tooling — the site itself is pure HTML)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local secrets / env
.env
.env.local
.env.*.local

# Logs
*.log

# Build artifacts (none for this project, but just in case)
dist/
build/
.cache/
